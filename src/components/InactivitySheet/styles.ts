import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  inativeSheetInnerContainer: {width: '100%', gap: 16},
  inativeSheetTitle: {textAlign: 'center', fontWeight: '600', fontSize: 24},
  inativeSheetDescription: {
    textAlign: 'center',
    fontWeight: '400',
    fontSize: 14,
  },
});
